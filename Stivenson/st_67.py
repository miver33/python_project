x: float = float(input("Введите первую координату x "))
y: float = float(input("Введите первую координату y "))

x_total: float = x
y_total: float = y
start_side: float =
x_next: float = float(input("введите координнату x "))
y_next: float = float(input("введите координнату y "))

while x_next != 0:
    x_next: float = float(input("введите координнату x "))
    y_next: float = float(input("введите координнату y "))
    x_total = x_total + x_next
    y_total = y_total + y
    side: float =


p: float = x_total * 2 + y_total * 2
print(p)

